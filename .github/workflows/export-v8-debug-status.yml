name: Export v8 debug status

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/export-v8-debug-status.yml'

permissions:
  actions: read
  contents: write

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Export latest v8 debug build status
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          JSON="$(gh api "repos/$REPO/actions/workflows/build-v8-debug-once.yml/runs?branch=main&per_page=1")"
          RUN_ID="$(printf '%s' "$JSON" | jq -r '.workflow_runs[0].id')"
          STATUS="$(printf '%s' "$JSON" | jq -r '.workflow_runs[0].status')"
          CONCLUSION="$(printf '%s' "$JSON" | jq -r '.workflow_runs[0].conclusion')"
          printf 'run_id=%s\nstatus=%s\nconclusion=%s\n' "$RUN_ID" "$STATUS" "$CONCLUSION" > latest-v8-debug-status.txt
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add latest-v8-debug-status.txt
          git commit -m 'Export v8 debug build status'
          git push
